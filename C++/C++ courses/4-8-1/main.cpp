#include <iostream>#include <algorithm>using namespace std;struct spectacol {    int start, finish;} a[101];int n;void sortare_spectacole(spectacol a[], int n) {    for(int i = 0; i < n - 1; i++) {        for(int j = i + 1; j < n; j++) {            if(a[i].finish > a[j].finish) {                swap(a[i], a[j]);            }        }    }}int main(){        cout<<"Numarul de spectacole: ";    cin>>n;        for(int i = 0; i < n; i++) {        cin>>a[i].start >> a[i].finish;    }        sortare_spectacole(a, n);    int contor = 1;    int ora_final = a[0].finish;        for(int i = 1; i < n; i++) {        if(ora_final <= a[i].start) {            contor++;            ora_final = a[i].finish;        }    }        cout<<"Numarul maxim de spectacole la care poate participa spectatorul este: "<<contor;        return 0;}    /*    La un festival, sunt programate n spectacole. Pentru fiecare spectacol, se cunoaste momentul de inceput     si momentul de sfarsit, exprimate prin numere naturale. Un spectator doreste sa urmareasca cat mai multe    spectacole in intregime. Determinati numarul maxim de spectacole care pot fi urmarite, fara ca acestea    sa se suprapuna.    */        /*         N = numarul de spectacole    x1 y1    x2 y2    ....    xn yn         N < 100     x1, y1 < 1 000 000    x < y         7 spectacole     2 4     8 11    5 6     5 8     3 7    7 8     9 12         Output-ul este 4: (2-4, 5-6, 7-8, 9-12)        */