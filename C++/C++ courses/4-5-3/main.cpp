#include <iostream>#include <vector>using namespace std;int main(){    /* Avem o matrice cu n linii si m coloane care contine numere intregi.    Sa se determine:     a) linia cu cea mai mare suma a elementelor    b) coloana cu cea mai mare suma a elementelor*/        int n, m;    cout<<"Introduceti numarul de linii(n): ";    cin>>n;    cout<<"Introduceti numarul de coloane(m): ";    cin>>m;        int matrice[n][m];        //citire matrice    for(int i = 0; i < n; i++) {        for(int j = 0; j < m; j++) {            cin>>matrice[i][j];        }    }        cout<<endl;        for(int i = 0; i < n; i++) {        for(int j = 0; j < m; j++) {            cout<<matrice[i][j]<<" ";        }        cout<<endl;    }    cout<<endl;    int suma_maxima_linii = -1;    //int linie_maxima = -1; - pentru o singura linie cu suma maxima    vector<int> linii_maxime;        for(int i = 0; i < n; ++i) {        int suma = 0;                for(int j = 0; j < m; ++j) {          suma = suma + matrice[i][j];         }                if(suma > suma_maxima_linii) {            suma_maxima_linii = suma;            //linie_maxima = i; - pentru o singura linie cu suma maxima            linii_maxime.clear();            linii_maxime.push_back(i);        }        else if(suma == suma_maxima_linii) {            linii_maxime.push_back(i);        }    }        //cout<<"Linia "<<linie_maxima + 1<<" are suma maxima: "<<suma_maxima_linii<<endl;        cout<<"Suma maxima a elementelor pe linie este: "<<suma_maxima_linii<<endl;        cout<<"Liniile cu aceasta suma maxima sunt: ";        for(int linie : linii_maxime) {        cout<<(linie + 1)<<" ";    }        cout<<endl;    int suma_maxima_coloane = -1;    int coloana_maxima = -1;        for(int j = 0; j < m; ++j) {        int suma = 0;                for(int i = 0; i < n; ++i) {            suma = suma + matrice[i][j];        }                if(suma > suma_maxima_coloane) {            suma_maxima_coloane = suma;            coloana_maxima = j;        }    }        cout<<"Coloana "<<coloana_maxima + 1<<" are suma maxima: "<<suma_maxima_coloane<<endl;    return 0;}